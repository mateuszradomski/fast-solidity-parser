contract C {
    address payable a;
    address payable public b;
    address payable[] c;
    address payable[] public d;
    mapping(uint => address payable hackBecauseImRight) e;
    mapping(uint => address payable[] hackBecauseImRight) f;
}
// ----
